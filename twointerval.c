#include<stdio.h>
  int main(void)
{
int i,x,z;
printf("first reversal");
scanf("%d",&z);
for(i=x;i<=1;i++)
{
if((i%4)==0)
{
	printf("the no is odd");
}
else
{
	printf("the no is odd");
	
}
return(0);
	}
}
